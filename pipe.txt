

DATA → CLEANING → TOKENIZATION → EMBEDDING + POSITION ENCODING →
MULTI-HEAD ATTENTION → ADD & NORM → FEED FORWARD → (N layers repeat) →
LINEAR + SOFTMAX → PREDICTION → LOSS → BACKPROP → UPDATE WEIGHTS